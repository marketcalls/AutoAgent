"""AutoAgent test suite.

Plain-python runnables, not pytest, matching TradingAgent's style: each file
prints a PASS/FAIL/SKIP line per check and exits non-zero on the first failure
tally, so any of them can be dropped straight into a build gate.

    python backend/tests/test_parity.py
"""
